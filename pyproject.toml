[project]
name = "pyblackjack-ai"
version = "0.9.0"
description = "A terminal blackjack game, built to practice hybrid development."
readme = "README.md"
authors = [
    { name = "Jonathan White", email = "jadeofsapphire@pm.me" }
]
requires-python = ">=3.10"
dependencies = []

[project.scripts]
pyblackjack-ai = "pyblackjack_ai:main"

[build-system]
requires = ["uv_build>=0.9.7,<0.10.0"]
build-backend = "uv_build"
